name: Blacksmith CI

on:
    push

jobs:
  build_image:
    
    runs-on: "internal-4vcpu-ubuntu-2204"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: test
        run: echo test
